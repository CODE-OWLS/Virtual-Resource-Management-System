from tkinter import *
from PIL import ImageTk,Image

def call():
  '''  forroot = Tk()
    forroot.title("VRMS")
    forroot.iconbitmap("images\codeowls.png")
    forroot.configure(bg="#24292e")
    forroot.geometry("180x300")
    def check():
        #if username and pass and dob match with each other proceed
        return 0

    u=Entry(forroot,text="Enter Email")
    p=Entry(forroot,text="Enter Mobile")
    l=Entry(forroot,text="Dob")
    ch= Button(forroot,text="Check", command=check)

    u.grid(row=0,column=0)
    p.grid(row=1,column=0)
    l.grid(row=2,column=0)
    rn.grid(row=3,column=0)



forroot.mainloop()'''

